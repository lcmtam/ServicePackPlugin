<% content_for :body do %>
  <%= form_for :project, url: "/projects" do |f| %>
    <%= f.label :name, 'Project name' %>
    <%= f.text_field :name %>
    <label for="createSP">Use SP?</label>
    <input type="checkbox" name="createSP"/><br/>
    <%= f.submit %>
  <% end %>
<% end %>